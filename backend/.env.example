# Environment variables for the backend application

# Server Configuration
PORT=3000

# Database Configuration (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=ghana_cabs_db

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key
JWT_EXPIRES_IN=1h # Example: 1 hour, 7d for 7 days

# Client URL (for CORS and Socket.IO, if applicable)
# CLIENT_URL=http://localhost:3001 # Example for a local web client
# CLIENT_MOBILE_URL=exp://... # Example for Expo Go

# Payment Gateway API Keys (Example for MTN)
# MTN_MOMO_API_KEY=your_mtn_api_key
# MTN_MOMO_API_SECRET=your_mtn_api_secret
# MTN_MOMO_CALLBACK_URL=https_your_api_domain_com/api/v1/payments/mtn/callback

# Add other environment specific variables here
# E.g., Google Maps API Key (if used server-side for some reason)
# GOOGLE_MAPS_API_KEY=your_google_maps_server_api_key
